import AboutLayoutClient from "../../components/AboutLayoutClient";

export default function AboutLayout({ children }: { children: React.ReactNode }) {
  return <AboutLayoutClient>{children}</AboutLayoutClient>;
}
